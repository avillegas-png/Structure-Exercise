import UIKit

struct engineTypes {
    var engine1 = "V4"
    var engine2 = "V6"
    var engine3 = "V8"
}

let engineChoice = engineTypes()

print("The engine options you have are a \(engineChoice.engine1) engine, a \(engineChoice.engine2) engine, and a \(engineChoice.engine3) engine")

